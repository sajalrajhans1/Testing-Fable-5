# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.*
!.env.example

# Python build artifacts (from skill scripts)
__pycache__/
*.pyc
.coverage

# Raw upstream clone of the ui-ux-pro-max skill. Its inner .claude/skills/*
# are already extracted into .claude/skills/, so this is a duplicate, and it
# carries its own .git (would commit as a broken gitlink). Re-fetch with:
#   git clone https://github.com/nextlevelbuilder/ui-ux-pro-max-skill
.claude/skills/ui-ux-pro-max-skill/
